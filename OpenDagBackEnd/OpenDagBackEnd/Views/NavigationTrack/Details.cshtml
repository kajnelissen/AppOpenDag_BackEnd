@model OpenDagBackEnd.Models.NavigationTrack

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>NavigationTrack</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Route.Name)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Route.Name)
    </div>
    <div class="display-field">
        <img src="@Url.Action("getImg", "NavigationTrack", new { id = Model.Id })" />
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
